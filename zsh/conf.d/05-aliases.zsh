# Aliases:
alias ls='ls --color'
alias tm='tmux attach || tmux new-session -s main'

# fold is a cool tool to wrap text, but by default it split words. Let it wrap
# at whitespace instead.
alias fold='fold --spaces'
