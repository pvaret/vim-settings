# Allow comments in interactive mode:
setopt interactivecomments

# Update the shared history file after each command. Each shell still only
# keeps its own history though, but newly started shells will have all the
# current history.
setopt inc_append_history_time
