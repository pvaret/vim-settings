# Enable ANSI colors.
# Quit right away if there's only one screen of text to show.
# Add a prompt at the bottom showing the current position in the file in terms
# of lines.
export LESS="-R --quit-if-one-screen -P%lb/%L"
